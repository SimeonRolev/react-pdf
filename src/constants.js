export const ZOOM_STEP = 0.1;

export class Mode {
    static NORMAL = {
        name: 'normal',
        cursor: 'default',
    };
    static PAN = {
        name: 'pan',
        cursor: 'grab',
    };
}
